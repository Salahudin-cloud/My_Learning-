fun main(args: Array<String>) {
    var name : String; // var can declare with the value , i meant we can filll the value later but we mus type the data type 
    name = "salahudin"; 

    var age = 18 ; 
    val address  = "Wates Magelang Utara"; 

    println(name); 
    println(age); 
    println(address); 
    /*we can change the value if we use var , if you want to change the value varibale but you are you using 
    val the code is gonna to erorr   */
    /* when we gonna use val ? we can use val hen we want if the value of variable no change we can use val example we want create 
    variable pi  */
    var nameSatu = "Salahudin"; 
    println("Hello , " + nameSatu); 
    val z : Int; 
    val x = 8 ; 
    val y = 2 ;
    z = x + y ; 
    println( z );  
}